## Asm
Learning Assembly and this repo is the collection of assembly language programs in order.

---
### Compile:

```sh
just build "name of the file"
```
This command will compile the project and runs it too
